# 이 저장소에서 도는 아침 브리핑 routine

이 저장소는 두 가지로 쓰인다.
1. GitHub Actions 수집기가 자막을 `transcripts/`와 `status.json`에 올린다. 이 두 경로는 수집기만 건드린다.
2. 매일 아침 예약 routine이 자막을 읽어 브리핑 메일을 보낸다. 아래 규칙은 이 routine에 적용된다.

## 작업을 멈출 때는 항상 알린다 (2026-09-26부터)

사용자 지시다. 끝까지 마치지 못하고 멈추는 모든 경우에 알린다. 조용히 멈추지 않는다.
- **해당하는 경우:** 사용자 결정이나 허락을 기다릴 때, 권한·도구·네트워크 오류로 막혔을 때, 09:00 KST 마감을 넘겼을 때,
  메일을 보내지 못했을 때, 단계를 건너뛰거나 기사 기반으로 대신했을 때, 세션이 늦게 시작돼 정시에 못 보낼 때.
- **예약 routine에서는** `PushNotification`으로 알린다. 첫 문장에 멈춘 곳과 이유를 쓰고, 이어서 끝낸 것, 남은 것, 사용자가 할 일을 쓴다.
- **대화형 세션에서는** 마지막 메시지 첫 줄에 "여기서 멈춤: <이유>"를 쓰고 사용자가 할 일을 적는다.
  사용자가 자리를 비웠을 수 있으면 `PushNotification`도 보낸다.
- 정상적으로 다 끝낸 날은 9절 한 줄 보고만 한다.

## 자막이 없을 때 — 기다리지 말고 수집기를 바로 돌린다 (2026-09-25부터)

프롬프트 4절의 "⏳ 수집기 지연 대기"(`sleep 600` 최대 3회)와 "먼저 올라왔는데 `.enc`가 없으면 바로 기사 기반" 규칙을 이 절이 대신한다.
routine 컨테이너는 youtube.com에 못 가므로 음성 받아쓰기는 GitHub Actions 수집기가 한다.
수집기는 영상마다 유튜브 자막을 먼저 시도하고, 자막 트랙이 없으면 같은 실행 안에서 바로 Whisper 음성 받아쓰기로 넘어간다.
그래서 자막이 없는 영상이 있으면 다음 예약 실행을 기다리지 않고 수집기를 지금 수동 실행한다.
**마감은 09:00 KST다.** 그때까지 확보하지 못한 영상은 기사 기반으로 쓰고 메일을 보낸다.

1. **확인한다.** 4절대로 clone한 뒤, 3절에서 확정한 대상 영상마다 `transcripts/<videoId>.enc`가 있고 `index.json`에서 `ok: true`인지 본다.
   전부 있으면 이 절은 건너뛴다. 하나라도 없으면 그 videoId들을 모아 2번으로 간다.
   - `status.json`의 해당 영상 `error`에 "받아쓰기 상한(90분) 초과"가 있어도 모은다. 음성으로는 못 받지만 수집기는 실행마다 자막부터 다시 시도하고,
     그 사이 자동 자막이 붙었을 수 있다. 실행 뒤에도 `MISSING`이면 5번의 재실행 없이 바로 기사 기반으로 쓰고 보고에 "90분 초과·자막 없음"을 적는다.
   - 3절 videos API의 `snippet.liveBroadcastContent`가 `upcoming`(예약만 된 방송)이면 대상에서 빼고 `notices`에 "예약 라이브 — 방송 전"으로 적는다.
   - `live`(방송 중)이면 수집기가 건너뛰므로, `sleep 600`을 **백그라운드로**(`run_in_background`) 돌리고 알림이 오면 videos API로 다시 확인한다.
     `none`이 되면 2번으로 간다. 3회(30분) 뒤에도 `live`면 그 영상은 기사 기반으로 쓰고 보고에 "방송 중"을 적는다.
2. **수집기를 수동 실행한다.** 먼저 실행 시각을 파일에 적는다. Bash 호출끼리 셸 변수가 이어지지 않으므로 반드시 파일로 남긴다.
   ```bash
   date -u +%Y-%m-%dT%H:%M:%SZ > /tmp/hkgm_T0
   ```
   바로 이어서 `mcp__github__actions_run_trigger`를 부른다.
   - `method`: `run_workflow`, `owner`: `lds96054341`, `repo`: `hkgm-transcripts`, `workflow_id`: `fetch-transcripts.yml`, `ref`: `main`, `inputs`: `{}`
   - `ref`는 반드시 `main`이다. 수집기는 결과를 main에 올리는데, 다른 브랜치로 돌리면 그 브랜치에 있는 검증되지 않은 워크플로 파일로 돈다.
   - `audio_test` 입력은 넣지 않는다. 그 결과는 `.audio-test.enc`로 저장되고 색인에 들어가지 않아 브리핑이 쓸 수 없다.
   - 도구가 없거나 권한 오류가 나면 수동 실행 없이 3번으로 간다. 다음 예약 실행이 결과를 올리면 3번이 잡는다. 보고에 실행 실패 사실을 적는다.
3. **결과를 기다린다.** 백그라운드로 돌린다(`run_in_background`). 끝나면 알림이 온다.
   ```bash
   bash /tmp/hkgm/briefing/wait_collector.sh --deadline-kst 09:00 <videoId> [videoId ...]
   ```
   2분 간격으로 확인하고, 45분이나 09:00 KST 중 이른 쪽까지 기다린다. 끝나면 `/tmp/hkgm`를 최신 clone으로 바꾸고
   영상마다 `OK caption|audio` 또는 `MISSING <상태> retry=<yes|no> <오류>`를 출력한다.
   도는·대기 중인 수집기 실행이 없는데 T0 이후 실행이 성공하지 못하고 끝났으면(GitHub API로 확인) 곧바로 멈춘다.
   대기 중인 수동 실행이 새 예약 실행에 밀려 취소된 경우는 실패로 보지 않고 새 실행을 기다린다.
   종료 코드는 0 모두 확보, 1 실행은 끝났지만 일부 없음, 2 마감 초과, 3 `/tmp/hkgm_T0` 오류, 4 수집기 실행 실패다.
   자막이 있으면 2~4분, 40분짜리 영상을 음성으로 받아쓰면 15분 안팎 걸린다. 기다리는 동안 5절 보조 자료를 모은다.
   - 스크립트가 없으면(`No such file`) 4절 clone을 2분 간격으로 백그라운드에서 되풀이해 `.enc`가 생기는지 본다. 마감은 같다.
4. **읽는다.** `OK`인 영상은 4절대로 복호화해 끝까지 읽는다. `/tmp/hkgm`가 바뀌었으므로 복호화는 이 뒤에 한다.
   - `audio`(Whisper 받아쓰기)도 영상 전문이다. 6절의 "자막이 없는 영상은 '말했다'라고 쓰지 말라"는 규칙은 전문이 아예 없는 영상에만 적용한다.
     다만 자막보다 오인식이 많으므로 고유명사와 숫자는 기사·검색으로 확인하고, 확인 안 된 수치에는 "음성 기준"을 붙인다.
   - 카드의 `basis`는 `자막 기반(유튜브 자막)` 또는 `음성 기반(Whisper 받아쓰기)`로 쓴다. `counts.caption`에는 둘 다 세고,
     음성이 있으면 `notices`에 "자막 기반 N건 중 음성 받아쓰기 M건"을 적는다.
5. **그래도 없으면.** `retry=yes`(라이브 대기, `PARTIAL`, 조각 누락 `fragment`)는 방송 직후 유튜브가 아직 처리 중이라는 뜻이다.
   바로 다시 돌리면 같은 결과가 나오므로 10분 쉬었다 다시 돌린다. **쉬기 전에 판단한다.** 지금 시각에 10분을 더해도 08:35 KST 이전이고
   재실행이 3회 미만이면 `sleep 600`을 백그라운드로 돌리고, 알림이 오면 `retry=yes`인 영상만 모아 2~3번을 다시 한다.
   그렇지 않으면 쉬지 않고 바로 다음 문장으로 간다.
   `retry=no`나 `retry=stale`(수동 실행이 커밋하지 못하고 끝나 상태가 T0 이전 것)이거나, 되풀이한 뒤에도 없거나, 마감이 지났으면
   그 영상만 기사 기반으로 쓰고 출력된 상태와 오류를 보고에 적는다.
   종료 코드 2·4면 `mcp__github__actions_list`(`list_workflow_runs`, `resource_id`: `fetch-transcripts.yml`)로 T0 이후 실행의 상태와 링크를 보고에 적는다.
6. **보고.** 9절 한 줄 보고의 "대기 횟수" 자리에 수집기 수동 실행 횟수와 영상별 결과(자막·음성·기사)를 적는다.

## 리포트 형식 (2026-09-25부터)

- routine 프롬프트의 1~7절(시간창, 대상 확정, 자막 확보, 보조 자료, 정확성 원칙, 리포트 내용 규칙)은 그대로 따른다.
  요약 3~5문장, 주제 단위 불릿 8~12개(1~2분 클립·쇼츠는 내용만큼), 타임스탬프 금지, `(기사)`·`(검색)` 표기, 종합 뷰 항목 모두 그대로다.
- **"오늘의 결론 3줄"은 불릿마다 메일에서 최대 3줄(130자 이하)이다** (2026-09-26부터). 수치는 핵심 한두 개만 남기고 나머지는 영상 카드로 넘긴다.
  렌더러가 130자를 넘는 결론을 검사에서 막는다.
- **영상 카드의 주요 내용 불릿도 하나당 메일에서 최대 4줄(굵은 결론 + 본문 합쳐 170자 이하)이다** (2026-09-26부터).
  프롬프트 7절의 "한 불릿 안에 2~3문장"을 이 규칙이 대신한다. 굵은 결론 한 문장에 근거 한두 문장을 붙이고, 수치는 핵심 한두 개만 남긴다.
  불릿 개수(8~12개)와 주제 단위로 묶는 원칙은 그대로다. 렌더러가 170자를 넘는 불릿을 검사에서 막는다.
- 8절의 메일은 **손으로 HTML을 쓰지 않고** 아래 절차의 렌더러로 만든다.
- 메일 본문에 시세 표, 핵심 요인, 막대 차트가 HTML 표로 그려져 들어간다. 차트에는 SVG나 이미지를 쓰지 않는다.
  영상 카드의 유튜브 썸네일은 프롬프트대로 남는다.
- claude.ai 비주얼 리포트는 소유자 계정에서만 열리는 **보조** 자료다. 수신자는 메일 본문만으로 모든 내용을 볼 수 있어야 한다.

### 프롬프트 8절 스타일 중 바뀐 것 (이 파일이 우선한다)

회사 메일이 배경색을 지우고 흰 글씨만 남겨 글씨가 사라진 일이 있었다(2026-09-24 확인). 그래서:
- 채널 구분 띠는 `#111`·`#1f4e79` **채운 배경 + 흰 글씨를 쓰지 않는다.**
  한경 글로벌마켓은 연회색 바탕 + 왼쪽 `#111` 굵은 줄 + 검은 글씨, 당잠사는 연하늘 바탕 + 왼쪽 `#1f4e79` 줄 + 남색 글씨다.
- 배지와 링크 버튼은 테두리 + 진한 글씨다. 메일 어디에도 흰 글씨(`color:#fff`)를 쓰지 않는다.
- 렌더러가 이렇게 그리므로 출력 HTML을 프롬프트 스타일에 맞춰 고치지 않는다.

### 절차

1. **데이터 파일을 쓴다.** 7절 규칙대로 쓴 내용을 `briefing/README.md` 형식의 JSON 하나로 만든다. 스크래치패드에 둔다.
   `/tmp/hkgm/briefing/examples/2026-09-24.json`을 복사해서 고치는 것이 가장 빠르다.
   복사했으면 `date`를 오늘(KST)로 바꾸고 모든 내용을 오늘 것으로 새로 쓴다. 날짜가 오늘이 아니면 렌더러가 멈춘다.
   `visual_url`은 비워 둔다(4번에서 넣는다).
   - `counts`: 근거별 건수. `{"caption": 자막·음성 기반 건수, "article": 기사 기반 건수}`.
   - `health_warnings`: 4절 건강성 점검에서 나온 "⚠️ ..." 문구를 그대로 넣는다(⚠️ 기호는 빼고). 없으면 빈 배열.
   - `notices`: "당잠사 미업로드" 같은 짧은 알림. 당잠사가 0건이면 `b_empty`에 "당잠사 미업로드"를 넣는다.
     한경 글로벌마켓이 0건이면 `a_empty`에 사유를 넣는다. 영상이 하나도 없는 날도 `videos: []`로 렌더링된다.
   - `tickers`: 직전 거래일 뉴욕 마감 6~8개. 검색으로 확인한 값만 쓴다.
   - `drivers`: 그날 핵심 서사가 "여러 요인 → 하나의 결과"로 정리될 때만 쓴다. 없으면 항목을 뺀다.
   - `charts`: 4~6개. 그날 영상에 실제로 나온 숫자로만 만든다. 등락률 `diverging` 하나는 기본으로 넣는다.
     자막에만 있고 확인 안 된 수치는 `note`에 "자막 기준"을 적는다. 숫자가 부족한 날은 차트 수를 줄인다.
2. **렌더링한다.** 4절에서 받은 최신 clone의 렌더러를 쓴다.
   ```bash
   python3 /tmp/hkgm/briefing/render.py <data.json> <출력폴더>
   ```
   검사에 실패하면(결론 3줄 아님, 결론 한 줄 130자 초과, 영상 불릿 170자 초과, 축 범위 밖 값, 타임스탬프, 금지 문자열 등) 메시지대로 JSON을 고치고 다시 돌린다.
3. **비주얼 리포트를 게시한다.** 출력폴더의 `visual.html`을 Artifact로 **새로** 게시한다(날짜마다 별도 URL).
   `icon`은 `chart`, `description`은 "M월 D일 한경 글로벌마켓·당잠사 아침 브리핑 비주얼 리포트".
   유튜브 썸네일(i.ytimg.com)은 Artifact에서 차단되므로 이 페이지는 텍스트 타일을 쓴다. 이미지를 넣으려 하지 않는다.
4. **URL을 넣고 다시 렌더링한다.** 게시 결과의 URL을 JSON `visual_url`에 넣고 2번을 다시 돌린다.
5. **메일을 한 번 보낸다.** `mcp__Gmail__send_message`
   - `subject`: `subject.txt` 내용 그대로
   - `body`: `email.txt` 전체, `htmlBody`: `email.html` 전체. 파일을 읽어 **한 글자도 줄이거나 생략하지 않고** 그대로 넣는다.
     `email.html`은 6만 자 안팎이다. 파일이 길어 도구 출력이 잘리면 여러 번 나눠 읽어 전부 확인한 뒤 넣는다.
   - 첨부 파일은 넣지 않는다. 메일 도구는 첨부를 base64 문자열로만 받아서 10만 자 안팎을 옮겨 적어야 하고,
     한 글자만 틀려도 파일이 깨진다.
   - 렌더러가 "OK"를 출력한 뒤에만 보낸다.
6. **9절 한 줄 보고**에 비주얼 리포트 URL을 덧붙인다.

### 게시가 실패하면

Artifact 게시가 실패해도 메일은 보낸다. `visual_url` 없이 렌더링하면 링크 버튼만 빠진다. 보고에 게시 실패 사실을 적는다.

### 렌더러가 실패하면

JSON을 고쳐도 렌더러가 계속 실패하면(파이썬 오류 등), 프롬프트 8절 방식대로 HTML을 직접 써서 보낸다.
이때도 위의 "바뀐 스타일"을 지킨다(채운 어두운 배경·흰 글씨 금지). 보고에 렌더러 실패 사실과 오류 메시지를 적는다.

## 저장소 쓰기 규칙

- 브리핑 routine은 이 저장소에 푸시하지 않는다. 데이터 JSON과 출력물은 스크래치패드에만 둔다.
  위 "자막이 없을 때" 절의 수집기 수동 실행(`actions_run_trigger`의 `run_workflow`, `ref: main`)만 허용한다.
  저장소에 쓰는 것은 수집기이고 routine은 실행만 요청한다. 실행 재시도·취소·로그 삭제 등 다른 Actions 조작은 하지 않는다.
- `briefing/`의 렌더러와 스타일을 바꾸는 작업은 브리핑과 별개의 작업으로 한다.
- 대화형 세션에서 사용자가 요청한 규칙·렌더러 변경 작업은 끝나면 **main에 병합**한다(사용자 지시, 2026-09-26).
  작업 브랜치에서 검사를 통과시킨 뒤 병합하고, 새로 clone한 main에서 반영을 확인한다. 이어서 테스트 메일을 한 통 보내 확인한다.
  테스트 메일 제목 앞에는 `[테스트]`를 붙인다. 이 규칙은 예약 브리핑 routine에는 해당하지 않는다(routine은 푸시하지 않는다).
