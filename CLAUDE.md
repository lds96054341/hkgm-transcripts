# 이 저장소에서 도는 아침 브리핑 routine

이 저장소는 두 가지로 쓰인다.
1. GitHub Actions 수집기가 자막을 `transcripts/`와 `status.json`에 올린다. 이 두 경로는 수집기만 건드린다.
2. 매일 아침 예약 routine이 자막을 읽어 브리핑 메일을 보낸다. 아래 규칙은 이 routine에 적용된다.

## 리포트 형식: 2026-09-25부터 비주얼 리포트를 함께 낸다

routine 프롬프트의 1~6절(시간창, 대상 확정, 자막 확보, 보조 자료, 정확성 원칙)은 그대로 따른다.
7~8절의 "메일 한 통" 산출물은 아래 절차로 만든다. 메일은 계속 보내며, 비주얼 리포트는 **추가**다.

### 절차

1. **데이터 파일을 쓴다.** 프롬프트 7절 규칙대로 쓴 내용(결론 3줄, 영상별 요약·불릿, 종합 뷰)을
   `briefing/README.md`의 형식에 맞춰 JSON 하나로 만든다. 스크래치패드 경로를 쓴다.
   예시는 `briefing/examples/2026-09-24.json` — 형식은 이 파일을 복사해서 고친다.
   - `tickers`: 직전 거래일 뉴욕 마감 6~8개. 검색으로 확인한 값만 쓴다.
   - `drivers`: 그날 핵심 서사가 "여러 요인 → 하나의 결과"로 정리되면 쓴다. 억지로 만들지 않는다(없으면 생략).
   - `charts`: 4~6개. 그날 영상에 실제로 나온 숫자로만 만든다. 등락률 `diverging` 하나는 기본으로 넣는다.
     자막에만 있고 확인 안 된 수치는 `note`에 "자막 기준"을 적는다. 숫자가 부족한 날은 차트 수를 줄인다.
2. **렌더링한다.** `python3 briefing/render.py <data.json> <출력폴더>`
   검사에 실패하면(결론 3줄 아님, 축 범위 밖 값, 타임스탬프, 금지 문자열 등) 메시지대로 JSON을 고치고 다시 돌린다.
3. **비주얼 리포트를 게시한다.** 출력폴더의 `visual.html`을 Artifact로 **새로** 게시한다(날짜마다 별도 URL).
   `icon`은 `chart`, `description`은 "M월 D일 한경 글로벌마켓·당잠사 아침 브리핑 비주얼 리포트".
   유튜브 썸네일(i.ytimg.com)은 Artifact에서 차단되므로 페이지는 텍스트 타일을 쓴다. 이미지를 넣으려 하지 않는다.
4. **URL을 넣고 다시 렌더링한다.** 게시 결과의 URL을 JSON `visual_url`에 넣고 2번을 다시 돌린다.
   메일 상단에 "차트로 보는 비주얼 리포트 열기" 버튼이 생긴다.
5. **메일을 한 번 보낸다.** `mcp__Gmail__send_message`
   - `subject`: `subject.txt` 내용 그대로
   - `body`: `email.txt`, `htmlBody`: `email.html` 내용 그대로
   - 첨부 파일은 넣지 않는다. 메일 도구는 첨부를 base64 문자열로만 받아서, 10만 자 안팎을
     그대로 옮겨 적어야 하고 한 글자만 틀려도 파일이 깨진다. 비주얼은 링크로만 전달한다.
   - 보내기 전에 렌더러가 "OK"를 출력했는지 확인한다. 렌더러가 실패한 상태로는 절대 보내지 않는다.
6. **9절 한 줄 보고**에 비주얼 리포트 URL을 덧붙인다.

### 게시가 실패하면

Artifact 게시가 실패해도 메일은 보낸다. `visual_url` 없이 렌더링하면 링크 버튼만 빠진다.
보고에 게시 실패 사실을 적는다.

## 저장소 쓰기 규칙

- 브리핑 routine은 이 저장소에 푸시하지 않는다. 데이터 JSON과 출력물은 스크래치패드에만 둔다.
- `briefing/`의 렌더러와 스타일을 바꾸는 작업은 브리핑과 별개의 작업으로 한다.
